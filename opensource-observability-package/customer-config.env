# ===============================================================================
# 🚨 OBSERVABILITY SUITE - CUSTOMER CONFIGURATION FILE
# ===============================================================================
#
# INSTRUCTIONS FOR CUSTOMER:
# 1. Fill in your details below (replace the placeholder values)
# 2. Save this file
# 3. Run: ./deploy-observability-stack.sh
# 4. That's it! No other files need to be touched.
#
# ARCHITECTURE: Clean Kubernetes Observability Stack
# ✅ Grafana handles: Dashboards + Monitoring
# ✅ Simple setup focused on observability
#
# ===============================================================================

# ============================================
# 🌐 KUBERNETES ENVIRONMENT
# ============================================
# Your Kubernetes node IP (for direct access - no port forwarding needed)
# Example: 10.122.28.111 or 192.168.1.100
KUBERNETES_NODE_IP=10.122.28.111

# Kubernetes namespace for observability stack
KUBERNETES_NAMESPACE=ao-os

# Alerting and notification integrations temporarily removed

# ============================================
# ⚙️ RESOURCE SIZING (Most Important)
# ============================================
# Cluster resource profile - prevents resource issues and pod crashes
# Options: small (2GB RAM total), medium (6GB RAM), large (12GB+ RAM)
CLUSTER_PROFILE=medium

# Storage Sizes (set high to prevent hanging)
GRAFANA_STORAGE_SIZE=10Gi             # Grafana dashboard data & config
TEMPO_STORAGE_SIZE=10Gi               # Distributed tracing data
SMOKEPING_STORAGE_SIZE=5Gi            # Network latency history
MTR_STORAGE_SIZE=2Gi                  # Network diagnostics data

# ============================================
# 📊 MONITORING SCOPE (Most Important)
# ============================================
# External endpoints to monitor with Blackbox Exporter
# Comma-separated list of URLs (leave empty if none)
MONITOR_EXTERNAL_URLS=https://yourapp.com,https://api.yourcompany.com

# Prometheus data collection and retention
SCRAPE_INTERVAL=15s                   # How often to collect metrics (15s, 30s, 1m)
DATA_RETENTION_DAYS=15                # How long to keep metrics data (affects storage)

# ============================================
# 🏢 COMPANY INFORMATION (Most Important)
# ============================================
# Organization details (appears in dashboards and monitoring)
COMPANY_NAME="Your Company Name"
ENVIRONMENT=production                # Options: dev, staging, production
CLUSTER_NAME=main-cluster             # Name of this Kubernetes cluster
COMPANY_SUPPORT_URL=https://support.yourcompany.com
COMPANY_TIMEZONE=UTC

# ===============================================================================
# END OF CONFIGURATION
# ===============================================================================
#
# NEXT STEPS:
# 1. Save this file after updating your values
# 2. Run: ./deploy-observability-stack.sh (does everything automatically)
#
# SUPPORT:
# - Documentation: See updated README.md
# - Service URLs after deployment:
#   • Grafana (Dashboards): http://YOUR_NODE_IP:30300
#   • Prometheus (Metrics): http://YOUR_NODE_IP:30090
#
# ===============================================================================