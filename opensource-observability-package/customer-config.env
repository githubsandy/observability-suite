# ===============================================================================
# 🚨 OBSERVABILITY SUITE - CUSTOMER CONFIGURATION FILE
# ===============================================================================
#
# INSTRUCTIONS FOR CUSTOMER:
# 1. Fill in your details below (replace the placeholder values)
# 2. Save this file
# 3. Run: ./apply-customer-config.sh
# 4. That's it! No other files need to be touched.
#
# ARCHITECTURE: Grafana-Native Alerting (No AlertManager needed!)
# ✅ Grafana handles: Dashboards + Alerting + Email + ServiceNow + WebEx
# ✅ Simpler setup with enterprise-grade notifications
#
# ===============================================================================

# ============================================
# 🌐 KUBERNETES ENVIRONMENT
# ============================================
# Your Kubernetes node IP (for direct access - no port forwarding needed)
# Example: 10.122.28.111 or 192.168.1.100
KUBERNETES_NODE_IP=10.122.28.111

# Kubernetes namespace for observability stack
KUBERNETES_NAMESPACE=ao-os

# ============================================
# 🎫 SERVICENOW INTEGRATION (Optional)
# ============================================
# Set to 'true' to enable ServiceNow incident creation for critical alerts
SERVICENOW_ENABLED=false

# ServiceNow Details (only fill if SERVICENOW_ENABLED=true)
SERVICENOW_INSTANCE=https://dev353745.service-now.com/api/now/table/incident?sysparm_fields=number
SERVICENOW_USERNAME=admin
SERVICENOW_PASSWORD=qcAL8SIe!2o!
SERVICENOW_ASSIGNMENT_GROUP="Incident Management"
SERVICENOW_CALLER_ID=system.observability

# ============================================
# 💬 CISCO WEBEX TEAMS INTEGRATION (Optional)
# ============================================
# Set to 'true' to enable WebEx Teams notifications
WEBEX_ENABLED=false

# WebEx Teams Details (only fill if WEBEX_ENABLED=true)
# Create bot at: https://developer.webex.com/my-apps
WEBEX_BOT_TOKEN=Mjk3NmZhOTYtZTlkYy00YTVkLTliYjUtMzMyZmI5N2U4Y2VhZGU4YmE5Y2MtYWQ4_PF84_1eb65fdf-9643-417f-9974-ad72cae0e10f
WEBEX_ROOM_ID=Y2lzY29zcGFyazovL3VzL0FQUExJQ0FUSU9OL2ZlNzlmM2E4LTFjMGItNGM1Yi05NDJlLTJmYmFiZGQ0MDA5Mw

# ============================================
# 🚨 ALERT CONFIGURATION
# ============================================
# Customize alert thresholds (optional - defaults are usually fine)
ALERT_POD_CRASH_THRESHOLD=5           # Number of pod restarts in 1 hour
ALERT_MEMORY_THRESHOLD=90             # Memory usage percentage (90%)
ALERT_DISK_THRESHOLD=10               # Free disk space percentage (10%)
ALERT_ERROR_RATE_THRESHOLD=0.1        # HTTP error rate per second
ALERT_RESPONSE_TIME_THRESHOLD=5       # Response time in seconds

# ============================================
# ⚙️ RESOURCE SIZING (Most Important)
# ============================================
# Cluster resource profile - prevents resource issues and pod crashes
# Options: small (2GB RAM total), medium (6GB RAM), large (12GB+ RAM)
CLUSTER_PROFILE=medium

# Storage Sizes (set high to prevent hanging)
GRAFANA_STORAGE_SIZE=10Gi             # Grafana dashboard data & config
TEMPO_STORAGE_SIZE=10Gi               # Distributed tracing data
SMOKEPING_STORAGE_SIZE=5Gi            # Network latency history
MTR_STORAGE_SIZE=2Gi                  # Network diagnostics data

# ============================================
# 📊 MONITORING SCOPE (Most Important)
# ============================================
# External endpoints to monitor with Blackbox Exporter
# Comma-separated list of URLs (leave empty if none)
MONITOR_EXTERNAL_URLS=https://yourapp.com,https://api.yourcompany.com

# Prometheus data collection and retention
SCRAPE_INTERVAL=15s                   # How often to collect metrics (15s, 30s, 1m)
DATA_RETENTION_DAYS=15                # How long to keep metrics data (affects storage)

# ============================================
# 🏢 COMPANY INFORMATION (Most Important)
# ============================================
# Organization details (appears in dashboards, alerts, and incident descriptions)
COMPANY_NAME="Your Company Name"
ENVIRONMENT=production                # Options: dev, staging, production
CLUSTER_NAME=main-cluster             # Name of this Kubernetes cluster
COMPANY_SUPPORT_URL=https://support.yourcompany.com
COMPANY_TIMEZONE=UTC

# ===============================================================================
# END OF CONFIGURATION
# ===============================================================================
#
# NEXT STEPS:
# 1. Save this file after updating your values
# 2. Run: ./apply-customer-config.sh
# 3. Run: helm upgrade ao-observability ./helm-kube-observability-stack --namespace ao-os
#
# SUPPORT:
# - Documentation: See updated README.md
# - Service URLs after deployment:
#   • Grafana (Dashboards + Alerting): http://YOUR_NODE_IP:30300
#   • Prometheus (Metrics): http://YOUR_NODE_IP:30090
#   • ServiceNow Webhook (if enabled): http://YOUR_NODE_IP:30950
#   • WebEx Webhook (if enabled): http://YOUR_NODE_IP:30951
#
# ===============================================================================